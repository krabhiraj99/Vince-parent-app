/* eslint no-console: "off" */

import Vue from "vue";

// settings

const actions = {};

const getters = {};

export default {
  actions,
  getters,
};

// export vue instance for external modules to extend.
export const VueInstance = Vue;
